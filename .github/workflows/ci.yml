name: CI

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo lint
      - run: pnpm turbo typecheck

  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile

      - name: Run tests
        run: pnpm turbo test
        env:
          SKIP_ENV_VALIDATION: "1"

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: lint
    env:
      SKIP_ENV_VALIDATION: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm turbo build

  security:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # Dependency vulnerability audit
      - name: Audit dependencies
        run: pnpm audit --audit-level=high || true

      # Check for known vulnerable packages
      - name: Check for critical vulnerabilities
        run: |
          AUDIT_OUTPUT=$(pnpm audit --json 2>/dev/null || echo '{}')
          CRITICAL=$(echo "$AUDIT_OUTPUT" | jq '.metadata.vulnerabilities.critical // 0' 2>/dev/null || echo "0")
          if [ "$CRITICAL" -gt 0 ] 2>/dev/null; then
            echo "::error::Found $CRITICAL critical vulnerabilities"
            exit 1
          fi

      # License compliance check
      - name: Check licenses
        run: |
          pnpm licenses list --prod --json > /tmp/licenses.json 2>/dev/null || true
          echo "License check completed — review /tmp/licenses.json for details"

      # CodeQL-style static analysis via ESLint security rules
      - name: Security linting
        run: |
          echo "Running security-focused lint checks..."
          pnpm turbo lint 2>&1 | tee /tmp/lint-output.txt
          if grep -qi "error" /tmp/lint-output.txt; then
            echo "::warning::Security lint found issues - review lint output"
          fi
